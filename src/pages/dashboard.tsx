import React from "react";
import { Header } from "../components";

export const DashboardView: React.FC = () => {
  return (
    <div className="container-fluid">
      <Header heading={"Board"} />
    </div>
  );
};
